import { ActionItemPipe } from './action-item.pipe';

describe('ActionItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionItemPipe();
    expect(pipe).toBeTruthy();
  });
});
